# 🚘 AvtoShop — Платформа для управления автосервисом

**AvtoShop** — это полнофункциональное веб-приложение, разработанное в рамках учебной практики для модернизации и улучшения цифрового присутствия автосервиса. Проект включает в себя публичный лендинг для привлечения клиентов и защищенную административную панель для управления внутренними процессами.

---

## 🛠️ Функционал

### Для обычных пользователей (лендинг):
- Просмотр каталога услуг и ключевых преимуществ компании.
- Изучение отзывов реальных клиентов.
- Возможность оставить собственный отзыв через интерактивную форму со звездным рейтингом.

### Для администраторов (админ-панель):
- **Защищенная аутентификация** по логину и паролю.
- **CRUD-операции** (создание, чтение, редактирование, удаление) для следующих сущностей:
  - 🧰 **Услуги**
  - 👥 **Клиенты**
  - ⭐ **Отзывы**
- **Поиск и сортировка** для быстрого доступа к данным.

## 🚀 Стек технологий

| Категория      | Технология                                                                  |
|----------------|-----------------------------------------------------------------------------|
| **Бэкенд**     | **Node.js**, **Express**, **pg** (драйвер PostgreSQL)                       |
| **Фронтенд**   | **React**, **React Router**, **Axios**                                      |
| **База данных**  | **PostgreSQL** (развернута на платформе **Supabase**)                      |
| **Аутентификация** | **JSON Web Tokens (JWT)**, **bcrypt.js** (для хеширования паролей)          |
| **Стилизация**   | **CSS** (с использованием методологии БЭМ)                                 |
| **Документация API**| **OpenAPI 3.0 (Swagger)**                                                   |
| **Инструменты**  | **Git**, **GitHub**, **nodemon**, **create-react-app**                       |

---

## 🖥️ Установка и запуск

Для полного развертывания проекта в режиме разработки выполните следующие шаги.

### Шаг 1: Клонирование репозитория

```bash
# Клонируйте репозиторий на свой компьютер
git clone <URL вашего репозитория>
cd <название папки проекта>
```

### Шаг 2: Настройка и запуск Бэкенда

Серверная часть требует подключения к базе данных и секретного ключа для JWT.

```bash
# 1. Перейдите в папку бэкенда
cd backend

# 2. Установите все необходимые зависимости
npm install

# 3. Создайте файл переменных окружения .env
# В Unix-системах (Linux, macOS) можно скопировать из примера:
cp .env.example .env
```

**4. Заполните `.env`:**
Откройте созданный файл `.env` и укажите в нем следующие переменные:

```env
# Строка для подключения к вашей базе данных PostgreSQL на Supabase
# (Настройки -> Database -> Connection string -> URI)
DATABASE_URL="postgres://user:password@host:port/database"

# Секретный ключ для подписи JWT-токенов. 
# Придумайте любую длинную и сложную случайную строку.
JWT_SECRET="your_super_secret_and_long_string_here"
```

**5. Запустите сервер:**

```bash
# Запуск сервера в режиме разработки с автоматическим перезапуском
npm start
```
> ✅ Сервер должен запуститься и быть доступным по адресу `http://localhost:5000`.

### Шаг 3: Настройка и запуск Фронтенда

Клиентская часть готова к работе "из коробки".

```bash
# 1. Вернитесь в корневую папку проекта
cd ..

# 2. Установите зависимости фронтенда
npm install

# 3. Запустите React-приложение
npm start
```
> ✅ Сайт автоматически откроется в вашем браузере по адресу `http://localhost:3000`.
> Запросы к API будут автоматически перенаправляться на бэкенд (`http://localhost:5000`) благодаря настройке `"proxy"` в файле `package.json`.

---

## 🏗️ Структура проекта

Проект имеет разделенную клиент-серверную архитектуру для лучшей модульности.

```
/
├── backend/         # Исходный код бэкенда (Node.js/Express)
│   ├── .env.example # Пример файла переменных окружения
│   ├── index.js     # Основной файл сервера, эндпоинты API
│   └── db.js        # Логика подключения к БД
├── docs/            # Папка с документацией
│   └── openapi.yaml # Спецификация API в формате OpenAPI 3.0
├── public/          # Статические ассеты для фронтенда (иконки, index.html)
├── src/             # Исходный код фронтенда (React)
│   ├── components/  # Переиспользуемые React-компоненты
│   ├── pages/       # Компоненты-страницы (Главная, Админ-панель и т.д.)
│   ├── styles/      # Глобальные стили
│   ├── App.js       # Корневой компонент приложения с роутингом
│   └── index.js     # Точка входа в React-приложение
├── .gitignore       # Файлы и папки, игнорируемые Git
├── package.json     # Зависимости и скрипты фронтенда
├── README.md        # Этот файл
└── TASKS.md         # Распределение задач по ролям в команде
```

## 📄 Документация API

Полное описание всех эндпоинтов, моделей данных и методов аутентификации представлено в формате **OpenAPI 3.0**.

- **Файл спецификации:** [**`/docs/openapi.yaml`**](./docs/openapi.yaml)

Вы можете использовать этот файл с любым совместимым инструментом (например, [Swagger Editor](https://editor.swagger.io/)) для визуализации, изучения и тестирования API.

## 👥 Команда и роли

Над проектом работает команда специалистов с четким разделением обязанностей. Подробное описание задач для каждой роли находится в файле [**TASKS.md**](./TASKS.md).
